export const createProject = function(project) {
    const requestOptions = {
        method: 'POST'
    };

    console.log(project);
    // return fetch(apiUrl + '/users/' + user.id + '/books?' + serialize(args), requestOptions)
    // .then((response) => {
    //     return response;
    // });
}